fn main(n: i32) -> i32 {
    let sum = 0;
    let items = [1, 2, 3, 4, 5];
    let index = 0;
    loop {
        if index == 5 {
            break;
        }
            sum += items[index];
            index += 1;
    }

    return sum;
}

assert main(123) == 15;